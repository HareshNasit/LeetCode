def commonChars(self, A):
        """
        :type A: List[str]
        :rtype: List[str]
        """
        #Brute Force
        #Create a dictionary for all all the strings to track the count of letters.
        #Compare and keep the count of each letter.
